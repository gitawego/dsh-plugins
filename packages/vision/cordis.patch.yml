# dsh-vision bundle patch: applied as one profile layer when this package joins a
# profile's `dsh.profile.bundles` (installed via `dsh plugin`).
- insert:
    - id: vision
      name: '@gitawego/dsh-vision'
